import { VerifyEmailPipe } from './verify-email.pipe';

describe('VerifyEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new VerifyEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
